import { NilToDashPipe } from './nil-to-dash.pipe';

describe('NilToDashPipe', () => {
  it('create an instance', () => {
    const pipe = new NilToDashPipe();
    expect(pipe).toBeTruthy();
  });
});
