export enum EnumGameStatus {
  Furture = 'furture',
  New = 'new',
  Draft = 'draft',
  Published = 'published',
}

export enum EnumGameMode {
  Init = 'init',
  Create = 'create',
  Edit = 'edit',
  Unknown = 'unknown',
}
